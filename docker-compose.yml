version: '3.8'
name: my-app
services:
  web:
    build: ./app          
    ports:
      - "5050:5050"
    depends_on:
      - redis
    environment:
      - FLASK_ENV=production

  redis:
    image: redis:7
    volumes:
      - redis_data:/data

volumes:
  redis_data:
